<%=form_for @sweet do |f|%>

  <%= f.collection_select :sweet_ids, Sweet.all, :id, :name %>
  <%= f.collection_select :vendor_ids, Vendor.all, :id, :name %>
  <%= f.label :price %>
  <%= f.text_field :price %>
  <%= f.submit "create"%>


<%end%>