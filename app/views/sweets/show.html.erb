<h1> Welcome to Our Sweet Page! </h1>
<h2>Sweet Name: <%= @sweet.name %></h2>
<h3>Here are the vendors that sell <%= @sweet.name %>s: </h3>
<ul>
    <% @sweet.vendors.each do |vendor| %>
        <li>
            <%= link_to vendor.name, vendor_path(vendor) %>
        </li>
    <% end %>
</ul>